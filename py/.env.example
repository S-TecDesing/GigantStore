# Copia este archivo como ".env" (sin ".example") en la carpeta py/ del
# servidor, y coloca ahí tu clave REAL de Groq. El archivo ".env" (sin
# .example) nunca se sube a GitHub — está excluido en .gitignore.
GROQ_API_KEY=coloca_aqui_tu_clave_real_de_groq
